import { NoCharacterInputDirective } from './no-character-input.directive';

describe('NoCharacterInputDirective', () => {
  it('should create an instance', () => {
    const directive = new NoCharacterInputDirective();
    expect(directive).toBeTruthy();
  });
});
